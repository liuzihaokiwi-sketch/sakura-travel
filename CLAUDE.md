# 项目总入口（每次会话必读）

> AI 进入这个项目第一份读的文档。瘦身到入口+导航·详情引用 docs/。

## 对用户的口号

> ### **一本为你写好的旅行手账**
> **旅行时带着走·旅行后留作回忆。**

所有用户可见文字（手账正文 / 文案 / 客服话术 / 小红书运营）的语气基线·都呼应这两句。

---

## 第一性原理（灰色判断时·任何决策回到这里）

项目必须同时满足四件事：

1. **内容来自专家策展与本地人视角**·不做信息堆砌
2. **交付物必须是一本精美、可随身翻阅的纸质手账**·而不是一份普通行程单
3. **用户体验必须是"照着走就对了"**·尽量少思考、少二次决策、少踩雷
4. **结果不只要好用**·还要让这趟旅行在结束后变成值得保存和反复翻看的回忆

**判断锚**：用户付费买的是"**有人替我想过 + 敢对结果负责**"的确定感。
任何决策回到这句：这件事是不是在加强这份确定感？

---

## 我们卖的不是信息·是「品质」+「确定性」+「独立」

AI 写攻略已经很简单·小红书/抖音/B 站攻略多到看不完·**信息从来不缺**。

- **品质**——专家策展 + 本地人视角写出来的·不是 AI 拼贴·不是搜索引擎扩写。精美的纸质手账让每一页都值得保存。
- **确定性**——用户不用自己比较、纠结、判断哪条攻略可信。**照着走就对了**·我们替你做完所有判断·并对结果负责。
- **独立**——**不和任何商家合作**·不收恰饭·不接返佣·不做品牌植入。每一家餐厅 / 酒店 / 景点的推荐·都是因为它真的好·不是因为它给我们钱。

详细哲学见 [docs/项目核心/项目定位.md](docs/项目核心/项目定位.md)。

---

## 5 秒理解

- **做什么**：付费旅行手账本·¥298 国内 / ¥348 国外·纸质本 + 贴纸 DIY 包
- **当前阶段**：关西样板间·京都/大阪数据池基本完成·酒店待重做
- **产品边界**：服务 < 10% 特殊用户的功能（二刷/奢华/已订酒店）默认走客服定制·主产品保持标准化简洁

**质量标准**：真实日本旅行顾问看到会不会点头·不点头就重做。

---

## 二、工作类型导航

### 🟢 做关西模板（当前主线）

**必读**：
- `docs/操作SOP/上线前/模板写作.md` — Part 0 策展 + Part 1 搜索 + Part 2 字段 + Part B 自检
- `japan/kansai/这是什么.md` — 关西定位 + 7 化学反应 + 区域基因 + 装配硬规
- `japan/kansai/research/insights.md` — 关西城市洞察

**🔗 联动**：挖到小众视角/10% 惊喜/本地冷知识 → `marketing/japan/kansai/素材库.md`

### 🟢 做研究

**必读**：`docs/操作SOP/上线前/研究方法.md` — 5 阶段 + 7 问 + MTE 框架

**🔗 联动**：opencli 搜到爆款 → `marketing/{地区}/爆款参考.md`

### 🟢 做数据采集（餐厅/stops/entity/酒店）

**必读**：
- `docs/操作SOP/数据采集.md` — 三轴判断 + 三层池 + 可信度分级（理论锚点）
- `docs/操作SOP/上线前/数据池构建/{餐厅,stops,entity,酒店}规范.md` — 4 类数据各自的字段+准入

**🔗 联动**：带传播性的冷知识/避坑 → 素材库

### 🟢 做装配（行程矩阵设计）

**必读**：`docs/操作SOP/上线前/装配设计.md`（草案版）

### 🟡 做运营内容

**必读**：`docs/操作SOP/上线后/内容运营客服.md` + `marketing/japan/kansai/素材库.md`

### 🟡 改架构 / 字段变更

**必读**：`docs/项目核心/历史决策.md` + `docs/项目核心/字段权威.md`

**硬规**：字段变更**先改 字段权威.md**·再改其他（字段权威是入口不是出口）

### 🟡 改代码

**必读**：`docs/项目核心/数据流.md` + `docs/项目核心/历史决策.md`

**注意**：app/ 里 import 路径对老 `content/kansai/` `data/events/` 失效·当前代码跑不起来。改代码时要同步路径。

### 🟡 客服/业务流程

**必读**：`docs/项目核心/业务流.md` + `docs/操作SOP/上线后/客服应对.md`

### 🟡 开新城市圈

**必读**：`docs/操作SOP/总流程.md` — 12 步骨架·上线前 9 + 上线后 3

---

## 三、6 条绝对红线（任何工作都不能破）

### 1. 数据真实性（第一红线）

适用：所有事实性内容——JSON 字段、文档元数据、素材库条目。

**可信度分级**：`verified`（2+ P0/P1 交叉）/ `cross_checked`（1 P0/P1+其他）/ `single_source`（仅 P2）/ `ai_generated`（**不可上生产**）

**自检**：写"事实"前问"真搜过的还是推断？"——推断的去真搜 / 或标"未核实"·**绝不把推断写成事实**。

**反例**：给餐厅填 `tabelog=4.3` 没搜过 Tabelog；写博主站"coverage=关西"没访问过；写"WebFetch 可用"没试过。

### 2. 不许打补丁绕过问题

修根因·不要 workaround。
- DB 连接失败 → 修连接·不加 `force_ai=True` 跳过
- 测试失败 → `xfail(strict=True)` 追踪·不改断言放水
- party_type 不识别 → 在定义本身携带属性·不在消费端列举

### 3. 不许硬编码业务数据

配置数据（城市圈映射/阈值/价格上限/人群分类）必须从配置文件或 DB 读。
**判断**：将来可能变的·不该在代码里。

### 4. 字段变更必须先改 字段权威.md

唯一权威源是 `docs/项目核心/字段权威.md`。顺序：字段权威 → 写作指引 → 历史决策。
**禁止反向**（先加字段再改）。

### 5. 不许假装修了实际没修

S/A 和 B/C 两分支写不同注释但做相同事（都 continue） → 逻辑相同写一个分支·不用注释掩盖。

### 6. 不许在函数体内重复 import

所有 import 放模块顶部。

---

## 四、产品第一性原理（灰色判断时的锚）

### 模板精髓（用户视角）

> **① 这不是一本攻略·是为我量身准备的一本日记。**
>
> **② 每翻一页·像有个懂当地的朋友在耳边跟我讲这段旅行。**

### 数据精髓

> **① 用户照着这条去现场·一切和本子一样。**
>
> **② 用户觉得"这家只有懂当地的人才会挑出来"。**

### 研究精髓

> **① 用户翻开觉得"懂这个地方"。**
>
> **② 走一趟回来觉得"我好像真的懂了这个国家"。**

所有 SOP 都是这些精髓的展开。灰色判断回到这几句。

---

## 五、关键目录速查

```
travel-ai/
├── japan/kansai/             ← 关西工作区（主战场）
│   ├── 这是什么.md            ← 关西定位+装配硬规
│   ├── 当前状态.md            ← 换窗口先读这个
│   ├── area_registry.json     ← area 白名单
│   ├── entities/              ← 景点 entity 数据
│   ├── restaurants/ stops/ hotels/ templates/  ← 数据
│   ├── assembly/              ← 装配规则+引擎
│   ├── research/              ← 关西研究产出
│   └── plans/                 ← 临时任务
├── japan/docs/                ← 日本特化文档（独立站清单）
├── japan/{kanto,hokkaido,...} ← 其他圈（未启动）
│
├── docs/
│   ├── 项目核心/              ← 字段权威/项目定位/业务流/数据流/历史决策
│   └── 操作SOP/
│       ├── 总流程.md           ← 12 步骨架·开新圈必读
│       ├── 数据采集.md         ← 跨 Phase 理论
│       ├── opencli使用.md      ← 工具
│       ├── 上线前/
│       │   ├── 研究方法.md
│       │   ├── 模板写作.md
│       │   ├── 装配设计.md
│       │   └── 数据池构建/{entity,餐厅,stops,酒店}规范.md
│       └── 上线后/
│           ├── pilot反馈.md
│           ├── 内容运营客服.md
│           └── 客服应对.md
│
├── marketing/                 ← 运营（素材库/爆款参考）
├── scripts/                   ← validator + 数据脚本
├── app/ web/                  ← 代码（部分待修）
└── _archive/                  ← 历史归档（不删）
```

---

## 六、工作方式（精简速记）

- **质量优先·简洁高效**：宁可少覆盖·不降低质量
- **信息整合 + 好的思考**：参考市面最佳（小红书/Black Tomato/Tabelog/BRUTUS）·不自己发明
- **当专家自主判断**：小事直接做·动产品形态时才问
- **有问题一次性提 3 个**·不逐个确认
- **Agent ≤2 并发**·简单用 haiku/sonnet·质量判断用 opus
- **Anthropic API 不高并发**（会限速）·高并发用阿里云 qwen-max
- **边做边沉淀素材** → marketing/ 素材库（产品护城河）

详细工作方式见 `MEMORY.md` 的 feedback 类笔记。

---

## 七、当前状态

关西进展 + 下一窗口接什么 → 读 `japan/kansai/当前状态.md`。

**当前已完成**：
- 京都 / 大阪 / kobe_area / other 餐厅 + stops 数据
- entity D43 三块结构重构
- 4 份数据池构建规范定稿
- 总流程.md + 装配设计.md（草案）

**当前待办**：
- 酒店数据重做（按 4 档 + city/experience 双标签）
- 各圈 entities 补缺
- 装配引擎实现

详见 `docs/项目核心/历史决策.md`。

---

## 八、工具速查

- **opencli**（小红书搜索/下载/正文）：完整用法见 `docs/操作SOP/opencli使用.md`
  - 搜索：`cd D:/projects/projects/travel-ai/opencli-main && node dist/main.js xiaohongshu search "xxx" --limit 10 --format md`
  - 下载图/视频：`node dist/main.js xiaohongshu download "完整URL带xsec_token" --output "D:/tmp/xhs_refs/..."`
  - 读正文：`node dist/main.js xiaohongshu note "完整URL" --format md`
  - ⚠️ note/download **串行跑**（并发会串数据）；URL 必须带 `xsec_token`；判断爆款看 **collects > likes**

- **validator**（每类数据校验）：
  - `python scripts/validate_entity.py`
  - `python scripts/validate_restaurants.py`（同时校验 restaurants + stops）
  - `python scripts/validate_hotels.py`
  - `python scripts/validate_template.py`

- **xhs 防盗链**：Referer `xiaohongshu.com` + iPhone UA（见 memory）

- **数据源优先级**：P0 官方/权威 → P1 中国用户源（小红书/携程）→ P2 参考 → P3 AI 兜底（不用于事实）

---

## 九、Git / 代码规范（改代码时看）

- 不主动 commit/push/force push/amend·除非用户明确要求
- commit message 写"为什么改"而不是"改了什么"
- 不加用户没要求的功能、重构、文档注释
- Bug 修复不"顺便"清理周围代码
- 不留死代码 / 注释掉的代码 / `# removed`

格式化：`ruff check --fix app/ scripts/ && ruff format app/ scripts/`

---

## 十、获取帮助

- `/help`（Claude Code）
- 反馈：https://github.com/anthropics/claude-code/issues
